{% extends 'base.html.twig' %}

{% block title %}- Placeholder{% endblock %}

{% block body %}
    <h1>Posts</h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Title</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for post in posts %}
                <tr>
                    <td>{{ post.title }}</td>
                        <td><a href="{{ path('posts_edit', {id: post.id}) }}">Edit</a></td>
                </tr>
            {% endfor %}
            <tr>
            </tr>
        </tbody>
    </table>
{% endblock %}
